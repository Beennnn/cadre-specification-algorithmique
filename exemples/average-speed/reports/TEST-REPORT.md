# Test report — SPEC-SPD-001

*Generated by `AverageSpeedTest`. Expected values come from the specification,
never from the implementation.*

## Reference cases

| Case | Result | Detail |
|---|---|---|
| `CT-01` | OK | 40.0 km/h · discriminates: the naive mean would give 45.0 km/h |
| `CT-02` | OK | 45.0 km/h · **does not discriminate** — the naive mean gives the same answer |
| `CT-03` | OK | 80.0 km/h · **does not discriminate** — the naive mean gives the same answer |
| `CT-04` | OK | 9.1 km/h · discriminates: the naive mean would give 10.0 km/h |
| `CT-05` | OK | rejected as expected — `E-SPD-001` |

## Property tested on generated inputs

| Property | Journeys | Result |
|---|---|---|
| `INV-01` — the average lies between the slowest and the fastest leg | 500 | OK |

## Coverage of what the specification requires

| Item | Statement | Covered |
|---|---|---|
| `RG-010` | Duration of one leg | yes |
| `RG-020` | Journey totals | yes |
| `RG-030` | Average speed | yes |
| `INV-01` | `average_speed` lies between the **slowest** and the **faste… | yes |
| `INV-02` | Before rounding, `average_speed × total_duration = total_dis… | yes |
| `E-SPD-001` | A leg has a speed `≤ 0` | yes |

**All reference cases pass, and every required item is covered.**